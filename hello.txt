try {
        const branch = await Branch.findOneAndUpdate(
            { "tables.number": "2" }, // Find the branch containing the table with number = 2
            { $pull: { tables: { number: "2" } } }, // Remove the table with number = 2
            { new: true }
        );
        console.log("Branch after deletion:", branch);
    } catch (error) {
        console.error("Error:", error);
    }
}